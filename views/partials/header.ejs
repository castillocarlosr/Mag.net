<header>
        <nav>
            <section class="brand">
                <a href="<%= '/' %>">Mag.Net</a>
            </section>
            <section>
                  <% for(let link of links){%> 
                    <% if(url === '/'){%>
                        <a href="#"><%= link ==='login'? '':'welcome John' %></a>
                    <% } else if(url === '/login'){ %>
                        <a href="#"><%= link ==='login'? '':'register' %></a>
                    <% } else { %>
                        <a href="#"><%= link ==='register'? '':'login' %></a>
                   <% } %>
                   
                <%} %> 
            </section>
        </nav>
    </header>
    